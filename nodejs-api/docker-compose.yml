version: "3"
services:
  postgresql:
    image: "postgres"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=cursos_pra_todos
    ports:
      - "5432:5432"